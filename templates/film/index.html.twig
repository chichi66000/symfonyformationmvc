{% extends 'base.html.twig' %}

{% block title %}Film accueil{% endblock %}

{% block body %}
    <div class="container ">
        <h1 class="text-center my-3">Film accueil</h1>

    {# <table class="table">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Description</th>
                <th>Date_de_sortie</th>
                <th>Duree</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for film in films %}
            <tr>
                <td>{{ film.titre }}</td>
                <td>{{ film.description }}</td>
                <td>{{ film.dateDeSortie ? film.dateDeSortie|date('Y-m-d') : '' }}</td>
                <td>{{ film.duree ? film.duree|date('H:i:s') : '' }}</td>
                <td>{{ film.image }}</td>
                <td>
                    <a href="{{ path('app_film_show', {'id': film.id}) }}">show</a>
                    <a href="{{ path('app_film_edit', {'id': film.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

        <div class="row ">
        {% for item in films %}
            <div class="card col-4 m-2" style="width: 15rem;">
                <img src="{{ item.image }}" class="card-img-top" alt="affiche">
                <div class="card-body">
                    <h5 class="card-title">{{item.titre}}</h5>
                    
                    <a href="{{ path('app_film_edit', {'id': item.id}) }}" class="btn btn-primary">Edition</a>
                    <a href="{{ path('app_film_show', {'id': item.id}) }}" class="btn btn-primary">Detail</a>
                </div>
            </div>
        {% endfor %}
        </div>
        <a href="{{ path('app_film_new') }}">Create new</a>
    </div>
{% endblock %}
